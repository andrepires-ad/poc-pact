//pactPublish.dependsOn test

pact {
	publish {
		pactDirectory = "target/pacts"
		pactBrokerUrl = "https://${pactBrokerHost}:${pactBrokerPort}"
//		pactBrokerUsername = System.properties['PACTBROKER_USERNAME']
//		pactBrokerPassword = System.properties['PACTBROKER_PASSWORD']
		tags = pactBrokerTags
	}
	reports {
		// adds the standard console output
		defaultReports()
		markdown
		json
	}
}

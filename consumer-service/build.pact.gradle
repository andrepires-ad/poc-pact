//pactPublish.dependsOn test

pact {
	publish {
		pactDirectory = "target/pacts"
		pactBrokerUrl = "https://pactbroker-snapshot.kops-ci.appdirect.tools:443"
		tags = pactBrokerTags
	}
	reports {
		// adds the standard console output
		defaultReports()
		markdown
		json
	}
}
